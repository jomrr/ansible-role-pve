---
# role: pve
# file: tasks/hardening/filesystems.yml

- name: "PVE | Ensure sticky bit on shared temporary directories"
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "1777"
  loop: "{{ __pve_hardening_tmp_acl_paths }}"
